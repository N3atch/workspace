# 🚀 מדריך הפעלה מפורט - מערכת חופשות

## שלב 1: הכנת מסד הנתונים MySQL

### 1.1 פתיחת MySQL
1. פתח את **MySQL Workbench** או **phpMyAdmin** או **Command Line** של MySQL
2. התחבר עם המשתמש שלך (בדרך כלל `root`)

### 1.2 יצירת מסד הנתונים
העתק והדבק את התוכן של הקובץ `backend/database/schema.sql` ב-MySQL:

**או** הרץ את הפקודות הבאות:
```sql
CREATE DATABASE IF NOT EXISTS vacation_db;
USE vacation_db;
```

### 1.3 יצירת הטבלאות
העתק את כל התוכן מהקובץ `backend/database/schema.sql` והרץ אותו ב-MySQL.

**או** הרץ את הפקודות הבאות:
```sql
CREATE TABLE IF NOT EXISTS users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    first_name VARCHAR(100) NOT NULL,
    last_name VARCHAR(100) NOT NULL,
    username VARCHAR(100) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    is_admin BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS vacations (
    id INT AUTO_INCREMENT PRIMARY KEY,
    description TEXT NOT NULL,
    destination VARCHAR(255) NOT NULL,
    image VARCHAR(255),
    start_date DATE NOT NULL,
    end_date DATE NOT NULL,
    price DECIMAL(10, 2) NOT NULL,
    followers_count INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS follows (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    vacation_id INT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (vacation_id) REFERENCES vacations(id) ON DELETE CASCADE,
    UNIQUE KEY unique_follow (user_id, vacation_id)
);

CREATE INDEX idx_follows_user ON follows(user_id);
CREATE INDEX idx_follows_vacation ON follows(vacation_id);
CREATE INDEX idx_vacations_dates ON vacations(start_date, end_date);
```

---

## שלב 2: הגדרת Backend (שרת)

### 2.1 יצירת קובץ .env
1. עבור לתיקייה: `Vacation-STUDENT_ID/backend/`
2. צור קובץ חדש בשם `.env` (ללא סיומת!)
3. העתק את התוכן הבא לקובץ:

```
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=הסיסמה_שלך_לMySQL
DB_NAME=vacation_db
JWT_SECRET=my-super-secret-key-change-in-production-12345
PORT=3001
FRONTEND_URL=http://localhost:3000
```

**⚠️ חשוב:** החלף את `הסיסמה_שלך_לMySQL` בסיסמה האמיתית שלך ל-MySQL!

### 2.2 יצירת משתמש אדמין
פתח טרמינל (PowerShell או CMD) ועבור לתיקיית backend:

```bash
cd Vacation-STUDENT_ID\backend
node database\initAdmin.js
```

אם הכל עבד, תראה הודעת הצלחה.

**פרטי התחברות לאדמין:**
- שם משתמש: `admin`
- סיסמה: `admin123`

---

## שלב 3: הפעלת השרת (Backend)

### 3.1 פתיחת טרמינל
פתח **טרמינל חדש** (PowerShell או CMD)

### 3.2 מעבר לתיקיית backend
```bash
cd Vacation-STUDENT_ID\backend
```

### 3.3 הפעלת השרת
```bash
npm start
```

**✅ אם הכל תקין, תראה:**
```
Server running on port 3001
```

**🔴 אל תסגור את החלון הזה!** השרת צריך להמשיך לרוץ.

---

## שלב 4: הגדרת Frontend (לקוח)

### 4.1 יצירת קובץ .env
1. עבור לתיקייה: `Vacation-STUDENT_ID/frontend/`
2. צור קובץ חדש בשם `.env` (ללא סיומת!)
3. העתק את התוכן הבא לקובץ:

```
REACT_APP_API_URL=http://localhost:3001/api
REACT_APP_SOCKET_URL=http://localhost:3001
```

---

## שלב 5: הפעלת הלקוח (Frontend)

### 5.1 פתיחת טרמינל נוסף
פתח **טרמינל חדש נוסף** (PowerShell או CMD)

**⚠️ חשוב:** זה צריך להיות חלון נפרד מהשרת!

### 5.2 מעבר לתיקיית frontend
```bash
cd Vacation-STUDENT_ID\frontend
```

### 5.3 הפעלת האפליקציה
```bash
npm start
```

**✅ אם הכל תקין:**
- הדפדפן ייפתח אוטומטית
- האפליקציה תרוץ על `http://localhost:3000`

---

## שלב 6: שימוש במערכת

### 6.1 התחברות כאדמין
1. בדפדפן, עבור ל: `http://localhost:3000`
2. תועבר אוטומטית לדף התחברות
3. התחבר עם:
   - שם משתמש: `admin`
   - סיסמה: `admin123`

### 6.2 יצירת משתמש חדש
1. לחץ על "אין לך חשבון? הירשם כאן"
2. מלא את כל השדות (חובה!)
3. לחץ "הירשם"

### 6.3 שימוש כאדמין
- **הוספת חופשה:** לחץ על "הוסף חופשה" → מלא פרטים → שמור
- **עריכת חופשה:** לחץ על אייקון העיפרון ✏️ על כרטיס החופשה
- **מחיקת חופשה:** לחץ על אייקון X 🗑️ על כרטיס החופשה
- **דוחות:** לחץ על "דוחות" בתפריט העליון

### 6.4 שימוש כמשתמש רגיל
- **עקוב אחרי חופשה:** לחץ על "עקוב אחרי" על כרטיס החופשה
- **הסר מעקב:** לחץ על "הסר מעקב"
- **עדכונים בזמן אמת:** כשאדמין משנה חופשה שאתה עוקב אחריה, השינוי יופיע אוטומטית!

---

## 🔧 פתרון בעיות נפוצות

### בעיה: "Cannot connect to database"
**פתרון:**
1. ודא ש-MySQL פועל
2. בדוק את הסיסמה בקובץ `.env` של backend
3. ודא שמסד הנתונים `vacation_db` קיים

### בעיה: "Port 3001 already in use"
**פתרון:**
1. סגור את כל החלונות של השרת
2. או שנה את הפורט בקובץ `.env` ל-3002

### בעיה: "Port 3000 already in use"
**פתרון:**
1. סגור את כל החלונות של React
2. או לחץ Y כששואלים אם להחליף פורט

### בעיה: "Module not found"
**פתרון:**
```bash
cd Vacation-STUDENT_ID\backend
npm install

cd ..\frontend
npm install
```

### בעיה: תמונות לא נטענות
**פתרון:**
1. ודא שתיקיית `upload` קיימת ב-root של הפרויקט
2. ודא שהשרת רץ

---

## 📋 סיכום - מה צריך להיות פתוח

כדי שהמערכת תעבוד, צריך **2 חלונות טרמינל פתוחים:**

1. **חלון 1 - Backend (שרת):**
   ```
   cd Vacation-STUDENT_ID\backend
   npm start
   ```
   צריך לראות: "Server running on port 3001"

2. **חלון 2 - Frontend (לקוח):**
   ```
   cd Vacation-STUDENT_ID\frontend
   npm start
   ```
   הדפדפן ייפתח אוטומטית

---

## ✅ בדיקת תקינות

### בדיקה 1: השרת עובד
פתח בדפדפן: `http://localhost:3001`
- אם אתה רואה שגיאה או "Cannot GET /" - זה תקין! השרת עובד.

### בדיקה 2: האפליקציה עובדת
פתח בדפדפן: `http://localhost:3000`
- צריך לראות דף התחברות

### בדיקה 3: מסד הנתונים עובד
התחבר ל-MySQL ובדוק:
```sql
USE vacation_db;
SELECT * FROM users;
```
- צריך לראות לפחות את משתמש האדמין

---

## 🎉 כל הכבוד!

אם הגעת עד כאן והכל עובד - המערכת מוכנה לשימוש!

**זכור:**
- השרת (backend) צריך לרוץ כל הזמן
- הלקוח (frontend) צריך לרוץ כל הזמן
- מסד הנתונים (MySQL) צריך לרוץ כל הזמן

**בהצלחה! 🚀**
