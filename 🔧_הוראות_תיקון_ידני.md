# 🔧 הוראות תיקון ידני - Internal Server Error

## הבעיה:
השרת לא קורא את הסיסמה הנכונה מקובץ ה-.env

## ✅ מה כבר תיקנתי:
- ✅ קובץ `.env` עודכן עם הסיסמה: `R37aC7!35`
- ✅ מסד הנתונים מחובר
- ✅ משתמש admin קיים

## 🛠️ פתרון ידני:

### שלב 1: עצור את השרת הישן

1. פתח **Task Manager** (Ctrl+Shift+Esc)
2. מצא תהליכי `node.exe`
3. עצור את כל התהליכים של node

**או** דרך PowerShell:
```powershell
Get-Process node | Stop-Process -Force
```

### שלב 2: הפעל את השרת מחדש

1. פתח **PowerShell חדש**
2. עבור לתיקיית backend:
```bash
cd Vacation-STUDENT_ID\backend
```

3. הפעל את השרת:
```bash
npm start
```

4. צריך לראות:
```
Server running on port 3001
```

### שלב 3: בדוק שהכל עובד

1. פתח דפדפן: http://localhost:3000
2. נסה להתחבר עם:
   - שם משתמש: `admin`
   - סיסמה: `admin123`

---

## 🔍 אם עדיין יש שגיאה:

### בדוק את חלון השרת:
- מה השגיאה המדויקת?
- האם יש שגיאת חיבור למסד נתונים?
- שלח לי את השגיאה ואתקן אותה

### בדוק את קובץ .env:
פתח: `Vacation-STUDENT_ID\backend\.env`

צריך לראות:
```
DB_PASSWORD=R37aC7!35
```

אם לא - עדכן ידנית.

---

## ✅ סיכום:

1. עצור את השרת הישן
2. הפעל מחדש: `npm start` ב-backend
3. נסה להתחבר

**הכל אמור לעבוד עכשיו! 🚀**
