# 📋 רשימת צעדים להפעלת הפרויקט

## ✅ מה כבר הותקן:
- ✅ כל התלויות של Backend (npm install הושלם)
- ✅ כל התלויות של Frontend (npm install הושלם)
- ✅ כל הקבצים נוצרו

## 🔧 מה צריך לעשות עכשיו:

### שלב 1: מסד נתונים MySQL (5 דקות)

**1.1 פתח MySQL:**
- פתח **MySQL Workbench** או **phpMyAdmin**

**1.2 הרץ את סכמת מסד הנתונים:**
- פתח את הקובץ: `backend/database/schema.sql`
- העתק את כל התוכן
- הדבק ב-MySQL והרץ

**או** הרץ את הפקודות הבאות ב-MySQL:
```sql
CREATE DATABASE IF NOT EXISTS vacation_db;
USE vacation_db;
```
ואז העתק את שאר הפקודות מהקובץ `schema.sql`

---

### שלב 2: יצירת קובץ .env ל-Backend (2 דקות)

**2.1 עבור לתיקייה:**
```
Vacation-STUDENT_ID\backend\
```

**2.2 צור קובץ חדש בשם `.env`** (ללא סיומת!)

**2.3 העתק את התוכן הבא:**
```
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=הסיסמה_שלך_לMySQL
DB_NAME=vacation_db
JWT_SECRET=my-super-secret-key-change-in-production-12345
PORT=3001
FRONTEND_URL=http://localhost:3000
```

**⚠️ חשוב:** החלף את `הסיסמה_שלך_לMySQL` בסיסמה האמיתית שלך!

---

### שלב 3: יצירת משתמש אדמין (1 דקה)

**3.1 פתח PowerShell או CMD**

**3.2 עבור לתיקיית backend:**
```bash
cd Vacation-STUDENT_ID\backend
```

**3.3 הרץ את הפקודה:**
```bash
node database\initAdmin.js
```

**✅ אם תראה:**
```
Admin user initialized successfully!
Username: admin
Password: admin123
```
**אז הכל עבד! ✅**

---

### שלב 4: הפעלת השרת - Backend (חלון 1)

**4.1 פתח PowerShell או CMD חדש**

**4.2 עבור לתיקיית backend:**
```bash
cd Vacation-STUDENT_ID\backend
```

**4.3 הפעל את השרת:**
```bash
npm start
```

**✅ צריך לראות:**
```
Server running on port 3001
```

**🔴 אל תסגור את החלון הזה!** השרת צריך להמשיך לרוץ.

---

### שלב 5: יצירת קובץ .env ל-Frontend (2 דקות)

**5.1 עבור לתיקייה:**
```
Vacation-STUDENT_ID\frontend\
```

**5.2 צור קובץ חדש בשם `.env`** (ללא סיומת!)

**5.3 העתק את התוכן הבא:**
```
REACT_APP_API_URL=http://localhost:3001/api
REACT_APP_SOCKET_URL=http://localhost:3001
```

---

### שלב 6: הפעלת הלקוח - Frontend (חלון 2)

**6.1 פתח PowerShell או CMD חדש נוסף**

**⚠️ חשוב:** זה צריך להיות חלון נפרד מהשרת!

**6.2 עבור לתיקיית frontend:**
```bash
cd Vacation-STUDENT_ID\frontend
```

**6.3 הפעל את האפליקציה:**
```bash
npm start
```

**✅ אם הכל תקין:**
- הדפדפן ייפתח אוטומטית
- האפליקציה תרוץ על `http://localhost:3000`
- תראה דף התחברות

---

### שלב 7: התחברות למערכת

**7.1 התחברות כאדמין:**
- שם משתמש: `admin`
- סיסמה: `admin123`

**7.2 יצירת משתמש חדש:**
- לחץ על "אין לך חשבון? הירשם כאן"
- מלא את כל השדות (חובה!)
- לחץ "הירשם"

---

## 🎯 סיכום - מה צריך להיות פתוח:

### ✅ 2 חלונות טרמינל:
1. **חלון 1:** Backend רץ (`npm start` ב-backend)
2. **חלון 2:** Frontend רץ (`npm start` ב-frontend)

### ✅ MySQL צריך לרוץ ברקע

### ✅ דפדפן פתוח על `http://localhost:3000`

---

## 🐛 פתרון בעיות:

### "Cannot connect to database"
→ בדוק את הסיסמה בקובץ `.env` של backend

### "Port 3001 already in use"
→ סגור את כל החלונות של השרת ונסה שוב

### "Module not found"
→ הרץ `npm install` בתיקיית backend או frontend

### תמונות לא נטענות
→ ודא שתיקיית `upload` קיימת ב-root של הפרויקט

---

## 📞 עזרה נוספת:

לקריאה מפורטת יותר, פתח את הקובץ:
- `מדריך_הפעלה_מפורט.md` - מדריך מפורט מאוד
- `README.md` - תיעוד כללי
- `QUICK_START.md` - התחלה מהירה

---

## 🎉 כל הכבוד!

אם הגעת עד כאן - המערכת מוכנה לשימוש!

**בהצלחה! 🚀**
